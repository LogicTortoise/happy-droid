# Architecture Decision Records

ADRs capture durable happy-droid architecture choices that have meaningful alternatives or cross-module consequences.

Use the next four-digit sequence number and the filename `NNNN-<slug>.md`. Each record contains:

- `Status`
- `Context`
- `Decision`
- `Consequences`
